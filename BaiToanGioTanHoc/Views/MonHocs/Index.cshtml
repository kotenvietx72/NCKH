@model IEnumerable<BaiToanGioTanHoc.Models.MonHoc>

@{
    ViewBag.Title = "Index";
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaMH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenMH)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaMH)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenMH)
            </td>
            <td>
                @if (User.IsInRole("Administrator"))
                {
                    @Html.ActionLink("Cập nhật", "Edit", new { id = item.MaMH }) @Html.Raw(" | ")
                    @Html.ActionLink("Xóa", "Delete", new { id = item.MaMH }) @Html.Raw(" | ")
                }
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaMH })
            </td>
        </tr>
    }

</table>
